<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.media_manager">

    <!--
    این پرمیشن‌ها به صورت خودکار توسط پلاگین مدیریت می‌شوند
    نیازی به درخواست دستی پرمیشن در کد نیست
    These permissions are automatically managed by the plugin
    No need to manually request permissions in code
    -->

    <!-- پرمیشن‌های جدید برای Android 13+ (API 33+) -->
    <!-- Modern permissions for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- پرمیشن‌های قدیمی برای Android 5-12 (API 21-32) -->
    <!-- Legacy permissions for Android 5-12 (API 21-32) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- دسترسی به تمام فایل‌ها - Android 11+ -->
    <!-- Access to all files - Android 11+ -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- پرمیشن اضافی برای نسخه‌های قدیمی Android -->
    <!-- Additional permission for older Android versions -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />

</manifest>
